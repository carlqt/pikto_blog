%h1 Listing blogs
= select_tag "blog-options", options_for_select(@options), prompt: "- Options -"
%table.table
  %thead
    %tr
      %th= check_box_tag "all_blogs"
      %th Title
      %th Content
      %th Status
      %th User
      %th{:colspan => "3"}
  %tbody
    = render @blogs

%br/
- if user_signed_in?
  = link_to 'New Blog', new_blog_path
